# GitHub repo + PAT are no longer set via environment variables — every
# visitor configures their own from inside the app itself (the "Connect
# your GitHub repository" popup on first use). Nothing to set here for that.
